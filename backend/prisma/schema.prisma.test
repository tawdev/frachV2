generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root@localhost:3306/meubles_db"
}

model Test {
  id Int @id @default(autoincrement())
}
